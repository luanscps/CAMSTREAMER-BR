package com.camera2rtsp

// Patch notes for Camera2Controller.kt
// A lógica base já está aplicada no repositório segundo o usuário.
// Este arquivo documenta a versão segura dos 3 toggles que devem existir em updateSettings():
//
// params["yuvCapture"]?.let  { if (it as Boolean) enableYuvProcessor()  else disableYuvProcessor()  }
// params["rawCapture"]?.let  { if (it as Boolean) enableRawCapture(currentWidth, currentHeight) else disableRawCapture() }
// params["depthFusion"]?.let { if (it as Boolean) enableDepthFusion(currentWidth, currentHeight) else disableDepthFusion() }
//
// O status usado pela WebGUI deve expor:
// yuv_enabled, raw_enabled, depth_enabled, last_yuv_ts_ns,
// depth_mean_mm, depth_min_mm, depth_max_mm.
