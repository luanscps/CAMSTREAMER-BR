<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0f172a"
    android:elevation="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Cabeçalho -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="20dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="CONFIGURAÇÕES"
                android:textSize="12sp"
                android:textColor="#f8fafc"
                android:textStyle="bold"
                android:letterSpacing="0.15"/>

            <ImageButton
                android:id="@+id/btnClosePanel"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="@drawable/bg_action_btn"
                android:src="@android:drawable/ic_menu_close_clear_cancel"
                android:scaleType="centerInside"
                android:padding="6dp"
                android:contentDescription="Fechar"/>

        </LinearLayout>

        <!-- ================================================ -->
        <!-- RTMP URL                                         -->
        <!-- ================================================ -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="SERVIDOR RTMP"
            android:textSize="9sp"
            android:textColor="#38bdf8"
            android:textStyle="bold"
            android:letterSpacing="0.2"
            android:layout_marginBottom="8dp"/>

        <EditText
            android:id="@+id/editRtmpUrl"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:hint="rtmp://ip:1935/live/stream"
            android:textSize="11sp"
            android:textColor="#f8fafc"
            android:textColorHint="#475569"
            android:background="@drawable/bg_spinner"
            android:paddingHorizontal="10dp"
            android:fontFamily="monospace"
            android:inputType="text"
            android:singleLine="true"
            android:layout_marginBottom="10dp"/>

        <Button
            android:id="@+id/btnApplyRtmpUrl"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:text="APLICAR E RECONECTAR"
            android:textSize="12sp"
            android:textColor="#000000"
            android:backgroundTint="#38bdf8"/>

        <!-- ================================================ -->
        <!-- VIEWS FANTASMAS — mantêm IDs que MainActivity.kt -->
        <!-- referencia em setupSettingsPanel(). Invisíveis.  -->
        <!-- ================================================ -->
        <Spinner  android:id="@+id/spinResolution" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>
        <Spinner  android:id="@+id/spinFrameRate"  android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>
        <Spinner  android:id="@+id/spinBitrate"    android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>
        <Switch   android:id="@+id/switchOis"      android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>
        <Switch   android:id="@+id/switchGrid"     android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>
        <Switch   android:id="@+id/switchHud"      android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>
        <Switch   android:id="@+id/switchAudio"    android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>
        <SeekBar  android:id="@+id/seekMicGain"    android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>
        <Button   android:id="@+id/btnResetParams" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone"/>

    </LinearLayout>
</ScrollView>
